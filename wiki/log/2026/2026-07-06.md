# Log 2026-07-06 ~ 2026-07-12

Wiki changes for this week, reverse chronological. See [.claude/rules/wiki-workflow.md](../../../.claude/rules/wiki-workflow.md) for the weekly split rule.

## 2026-07-07

### Ported evolved wiki mechanics back from a downstream project

Restructured the template to match mechanics proven in a downstream fork:

- Index is now two-level: `index.md` holds a category table (page count, sub-index link, keywords); per-category pages live in `index-<slug>.md`.
- Change log moved from the single `log.md` to this weekly file tree (`log/<year>/<monday>.md`); the old sliding-window/archive idea is retired — new weeks open new files, old files stay put.
- SessionStart now injects a category summary plus a lookup workflow instead of the full index.
- Write gate hardened: weekly log tree and listed sub-indexes are maintenance-exempt (fail-closed), Bash detection normalizes case/quotes/backslashes and catches redirect/`tee` writes; legacy `log.md` keeps its exemption for older installs.
- Added `.claude/scripts/wiki-search.sh` (tag + full-text search over knowledge pages).
